<% provide(:title, 'All Subjects') %>
<h1><%= t(:all_subjects_text) %></h1>

<%= will_paginate %>

<table class="table">
  <thead>
    <th><%= t(:name) %></th>
    <th><%= t(:description) %></th>
  </thead>

  <%= render @subjects %>

</table>

<%= will_paginate %>
