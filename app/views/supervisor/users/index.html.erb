<% provide(:title, params[:role].pluralize) %>
<h1>All <%= params[:role].pluralize %></h1>

<%= will_paginate %>

<ul class="users">
  <ul class="users">
    <%= render @users %>
  </ul>
</ul>

<%= will_paginate %>
