<li>
  <%= gravatar_for user, size: 50 %>
  <%= link_to user.name, user %>
  <% if !user.supervisor? %>
    | <%= link_to "delete", supervisor_user_path(user), method: :delete,
                                  data: { confirm: t(:confirm_message) } %>
  <% end %>
</li>
